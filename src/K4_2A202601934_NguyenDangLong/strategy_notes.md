# Nguyen Dang Long - Heading and recursive fallback

This strategy starts a new section at each Markdown heading and preserves the heading in every child chunk.
Long sections fall back to the shared recursive separators so policy conditions remain readable.
